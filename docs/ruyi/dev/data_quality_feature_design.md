# 数据质量检查功能设计文档

## 1. 功能概述

通过 JinRefine 项目界面的 `extension-bar` 扩展功能，实现数据质量检查能力，帮助用户验证项目数据的格式、内容正确性、资源关联性等。

**本功能作为独立扩展（data-quality）实现**，不依赖 record-db 扩展，但可与其协同工作。

### 1.1 术语定义

| 中文术语 | 英文术语 | 说明 |
|---------|----------|------|
| 题名 | Title | 档案文件的标题 |
| 文件编号/文号 | Original Identifier | 原始标识符，常著录于附注或专门扩展项 |
| 日期 | Dates | 成文日期、归档日期等 |
| 责任者 | Creator Name | 文件的创建者/责任人 |

### 1.2 错误与警告定义

| 级别 | 英文 | 定义 | 示例 |
|------|------|------|------|
| **错误 (Error)** | Error | 明确违反规则，数据不合格，必须修复 | 唯一性冲突、非空字段为空、正则不匹配、文件缺失 |
| **警告 (Warning)** | Warning | 可能存在问题，建议人工复核 | 内容比对相似度低于阈值但不为0、文件数量差异在容许范围内、日期格式可解析但非标准格式 |
| **通过 (Pass)** | Pass | 完全符合规则要求 | 所有检查项通过 |

**警告产生场景**：
- 内容比对：OCR 抽取结果与数据字段相似度在 50%-90% 之间（可配置阈值）
- 资源检查：文件数量差异在 ±1 范围内
- 格式检查：日期可解析但格式不完全匹配（如 "2025/12/05" vs "2025-12-05"）

## 2. 功能需求

### 2.1 菜单结构

在 `extension-bar` 中添加"数据质量"下拉菜单，包含三个子菜单：

| 菜单项 | 功能描述 |
|--------|----------|
| 编辑质量规则 | 在 tool-panel 展开质量规则编辑 Tab |
| 管理质量规则 | 弹出模态对话框管理历史规则 |
| 立即检查... | 启动质量检查任务并展示结果 |

### 2.2 质量规则类型

#### 2.2.1 格式检查规则（按列）
- **唯一性检查**：检测列值是否唯一
- **非空检查**：检测空值
- **值域检查**：
  - 自定义值列表（枚举值）
  - 日期格式正确性
  - 正则表达式匹配

#### 2.2.2 内容正确性检查
- 通过 Jinshu-aimp OCR 接口从资源文件抽取信息
- 将抽取结果与项目数据进行比对

#### 2.2.3 文件资源关联性检查
- **文件数量一致性**：验证关联资源文件数量和条目中的页数数量是否匹配
- **文件夹格式**：检查关联文件夹命名结构
- **文件夹顺序连续性**：检查文件夹名称中序号是否连续
- **文件名格式**：验证文件命名规范
- **文件顺序连续性**：检查文件名中后缀编号部分是否连续

## 3. 技术架构

### 3.1 前端架构（参照 wikibase 扩展）

```
extensions/data-quality/
├── module/
│   ├── MOD-INF/
│   │   └── controller.js           # 扩展注册入口
│   ├── scripts/
│   │   ├── menu-bar-extension.js   # ExtensionBar 菜单注册
│   │   ├── quality-alignment.js    # 主模块（类似 SchemaAlignment）
│   │   ├── quality-rules-tab.html  # 规则编辑 Tab HTML
│   │   ├── quality-results-tab.html# 检查结果 Tab HTML
│   │   └── dialogs/
│   │       ├── manage-rules-dialog.js   # 管理规则对话框
│   │       └── run-check-dialog.js      # 运行检查对话框
│   ├── styles/
│   │   └── quality-alignment.css
│   └── langs/
│       ├── translation-en.json
│       └── translation-zh.json
└── src/
    └── com/google/refine/extension/quality/
        ├── QualityController.java      # 扩展控制器
        ├── commands/
        │   ├── SaveRulesCommand.java   # 保存规则
        │   ├── ListRulesCommand.java   # 获取规则列表
        │   ├── RunCheckCommand.java    # 运行检查
        │   └── GetResultsCommand.java  # 获取结果
        ├── model/
        │   ├── QualityRule.java        # 规则模型
        │   ├── CheckResult.java        # 检查结果
        │   └── QualityTask.java        # 检查任务
        └── services/
            ├── RuleValidator.java      # 规则验证器
            ├── ContentChecker.java     # 内容检查器
            └── AimpClient.java         # Jinshu-aimp 客户端
```

### 3.2 Tab 扩展机制

参照 wikibase 的 `SchemaAlignment.setUpTabs()` 实现：

```javascript
QualityAlignment.setUpTabs = function() {
  this._rightPanel = $('#right-panel');
  this._viewPanel = $('#view-panel').addClass('main-view-panel-tab');
  this._toolPanel = $('#tool-panel');
  this._summaryBar = $('#summary-bar')
    .addClass('main-view-panel-tab-header')
    .addClass('active')
    .attr('href', '#view-panel');

  // 创建质量规则 Tab
  this._rulesPanel = $('<div id="quality-rules-panel"></div>')
    .addClass('main-view-panel-tab')
    .appendTo(this._rightPanel);

  // 创建检查结果 Tab
  this._resultsPanel = $('<div id="quality-results-panel"></div>')
    .addClass('main-view-panel-tab')
    .appendTo(this._rightPanel);

  // 添加 Tab 头部
  var rulesButton = $('<div></div>')
    .addClass('main-view-panel-tab-header')
    .attr('href', '#quality-rules-panel')
    .text('质量规则')
    .appendTo(this._toolPanel);

  var resultsButton = $('<div></div>')
    .addClass('main-view-panel-tab-header')
    .attr('href', '#quality-results-panel')
    .text('检查结果')
    .appendTo(this._toolPanel);
};
```

### 3.3 后端 API

#### 规则管理 API

| 端点 | 方法 | 描述 |
|------|------|------|
| `/command/data-quality/save-rules` | POST | 保存质量规则（项目级或共享） |
| `/command/data-quality/list-rules` | GET | 获取规则列表（含共享规则） |
| `/command/data-quality/get-rule` | GET | 获取单个规则详情 |
| `/command/data-quality/delete-rule` | POST | 删除规则 |

#### 任务管理 API（支持断点续跑）

| 端点 | 方法 | 描述 | 参数 |
|------|------|------|------|
| `/command/data-quality/run-check` | POST | 启动检查任务 | `ruleId`, `projectId` |
| `/command/data-quality/pause-task` | POST | 暂停任务（保存断点） | `taskId` |
| `/command/data-quality/resume-task` | POST | 恢复任务（从断点继续） | `taskId` |
| `/command/data-quality/cancel-task` | POST | 取消任务 | `taskId` |
| `/command/data-quality/task-status` | GET | 获取任务状态和进度 | `taskId` |
| `/command/data-quality/list-tasks` | GET | 获取项目的任务列表 | `projectId` |

#### 结果查询 API（支持分页）

| 端点 | 方法 | 描述 | 参数 |
|------|------|------|------|
| `/command/data-quality/get-results` | GET | 获取检查结果（分页） | `taskId`, `offset`, `limit`, `severity`, `category`, `column` |
| `/command/data-quality/get-summary` | GET | 获取统计摘要 | `taskId` |
| `/command/data-quality/export-excel` | POST | 导出带标注 Excel | `taskId` |
| `/command/data-quality/export-pdf` | POST | 导出 PDF 报告 | `taskId` |
| `/command/data-quality/export-stats` | POST | 导出统计报表 | `taskId`, `format` |

### 3.4 与 Jinshu-aimp 集成

通过 HTTP 调用 jinshu-aimp 的 OCR 接口进行内容抽取，服务地址通过配置文件指定：

```java
// AimpClient.java
public class AimpClient {
    // 从配置文件读取
    private String aimpServer = ConfigurationProperties.get("jinshu.aimp.server", "http://127.0.0.1:7998");
    
    public ExtractResult extractLabels(String imagePath, List<String> tags) {
        // POST /ocr/classic 进行 OCR
        // POST /extract/labels 进行标签抽取
    }
}
```

### 3.5 数据持久化

质量规则和检查结果持久化到项目数据目录：

```
workspace/
└── {project-id}.project/
    ├── data.zip              # 原有项目数据
    ├── metadata.json         # 原有项目元数据
    └── quality/              # 质量检查数据目录（新增）
        ├── rules.json        # 当前项目的规则配置
        ├── tasks/            # 检查任务历史
        │   ├── {task-id}.json
        │   └── ...
        └── results/          # 检查结果
            ├── {task-id}.json
            └── ...
```

全局共享规则存储在工作空间根目录：

```
workspace/record-db/
└── quality-rules/           # 全局共享规则目录
    ├── {rule-id}.json
    └── ...
```

## 4. 数据模型

### 4.1 预置规则模板模型

预置规则模板以 JSON 配置文件形式存储在扩展目录 `extensions/data-quality/templates/`：

```json
// format-template-2022.json - 档案著录规则-2022版
{
  "id": "format-2022",
  "name": "文书档案著录规则-2022版",
  "category": "format",
  "enabled": true,
  "description": "适用于2022年档案著录规范的数据格式检查规则",
  "columnRules": [
    {
      "columnName": "档号",
      "checks": [
        { "type": "non_empty" },
        { "type": "unique" },
        { "type": "regex", "pattern": "^[A-Z]\\d{6}$", "description": "大写字母+6位数字" }
      ]
    },
    {
      "columnName": "题名",
      "checks": [{ "type": "non_empty" }]
    },
    {
      "columnName": "日期",
      "checks": [
        { "type": "non_empty" },
        { "type": "date_format", "format": "yyyy-MM-dd" }
      ]
    },
    {
      "columnName": "责任者",
      "checks": [{ "type": "non_empty" }]
    }
  ]
}

// content-template-doc-elements.json - 文书档案要素比对规则
{
  "id": "content-doc-elements",
  "name": "文书档案要素比对规则",
  "category": "content",
  "enabled": true,
  "description": "4个关键要素：题名、责任者、文号、成文时间",
  "contentRules": [
    { "columnName": "题名", "extractionTag": "题名", "similarity": 0.9 },
    { "columnName": "责任者", "extractionTag": "责任者", "similarity": 0.9 },
    { "columnName": "文号", "extractionTag": "文号", "similarity": 0.95 },
    { "columnName": "成文时间", "extractionTag": "成文时间", "similarity": 1.0 }
  ]
}
```

### 4.2 质量规则模型

```json
{
  "id": "uuid",
  "name": "规则名称",
  "shared": true,
  "createdTime": "2025-12-05T10:00:00Z",
  "appliedTemplates": [
    { "templateId": "format-2022", "appliedAt": "2025-12-05T10:00:00Z" },
    { "templateId": "content-doc-elements", "appliedAt": "2025-12-05T10:00:00Z" }
  ],
  "rules": [
    {
      "type": "format",
      "columnName": "档号",
      "checks": [
        { "type": "non_empty" },
        { "type": "unique" },
        { "type": "regex", "pattern": "^[A-Z]\\d{6}$" }
      ]
    },
    {
      "type": "format",
      "columnName": "归档日期",
      "checks": [
        { "type": "date_format", "format": "yyyy-MM-dd" }
      ]
    },
    {
      "type": "content",
      "columnName": "题名",
      "source": {
        "type": "ocr_extraction",
        "resourceColumn": "路径",
        "extractionTag": "题名"
      },
      "similarity": 0.9
    },
    {
      "type": "resource",
      "columnName": "路径",
      "checks": [
        { "type": "folder_exists" },
        { "type": "file_count", "expectedColumn": "件数" },
        { "type": "file_name_pattern", "pattern": "^\\d{4}\\.tif$" },
        { "type": "file_sequence_continuous" }
      ]
    }
  ]
}
```

### 4.2 检查任务模型（支持断点续跑）

```json
{
  "taskId": "uuid",
  "ruleId": "规则ID",
  "projectId": "项目ID",
  "status": "pending|running|paused|completed|failed|cancelled",
  "progress": {
    "totalRows": 10000,
    "processedRows": 7500,
    "percentage": 75,
    "currentPhase": "format_check",
    "phases": {
      "format_check": { "status": "completed", "processedRows": 10000 },
      "resource_check": { "status": "running", "processedRows": 7500 },
      "content_check": { "status": "pending", "processedRows": 0 }
    }
  },
  "checkpoint": {
    "lastProcessedRowIndex": 7499,
    "lastPhase": "resource_check",
    "savedAt": "2025-12-05T10:03:00Z"
  },
  "startTime": "2025-12-05T10:00:00Z",
  "pausedTime": null,
  "resumedTime": null,
  "endTime": null,
  "errorMessage": null
}
```

**断点续跑机制**：
- 任务按批次处理（默认 100 行/批），每批完成后保存 checkpoint
- checkpoint 记录：最后处理的行索引、当前阶段、保存时间
- 暂停时立即保存当前状态到 `tasks/{task-id}.json`
- 恢复时从 checkpoint 指定位置继续，已检查的结果不重复处理
- 服务重启后自动检测未完成任务，可选择继续或重新开始

### 4.3 检查结果模型

```json
{
  "taskId": "uuid",
  "ruleId": "规则ID",
  "projectId": "项目ID",
  "summary": {
    "totalRows": 10000,
    "errorCount": 500,
    "warningCount": 200,
    "passedRows": 9300,
    "byType": {
      "format": { "errors": 150, "warnings": 50 },
      "resource": { "errors": 200, "warnings": 100 },
      "content": { "errors": 150, "warnings": 50 }
    }
  },
  "issues": [
    {
      "rowIndex": 5,
      "columnName": "档号",
      "checkType": "unique",
      "category": "format",
      "severity": "error",
      "message": "值重复，与第3行相同"
    },
    {
      "rowIndex": 30,
      "columnName": "题名",
      "checkType": "content_match",
      "category": "content",
      "severity": "warning",
      "message": "相似度78%，低于阈值90%",
      "details": {
        "expected": "关于土地确权的通知",
        "actual": "土地确权通知",
        "similarity": 0.78
      }
    }
  ],
  "generatedAt": "2025-12-05T10:05:00Z"
}
```

**结果存储**：
- 问题列表单独存储在 `results/{task-id}.json`
- 大数据量时分片存储：`results/{task-id}_part1.json`, `results/{task-id}_part2.json`
- API 支持分页查询：`GET /get-results?taskId=xxx&offset=0&limit=50&severity=error`

## 5. 界面设计

### 5.1 质量规则 Tab

采用**二级 Tab 横向平铺**布局，将三类检查规则分别放在独立的子 Tab 中：

```
┌─────────────────────────────────────────────────────────────┐
│ [数据] [质量规则] [检查结果]                                  │  ← 一级 Tab
├─────────────────────────────────────────────────────────────┤
│ 规则名称: [________________] ☑共享规则 [保存] [另存为...]     │
├─────────────────────────────────────────────────────────────┤
│ [数据格式检查] [文件资源关联检查] [内容比对检查]               │  ← 二级 Tab
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─ 数据格式检查 ──────────────────────────────────────────┐│
│  │                                                         ││
│  │ 规则模板: [文书档案著录规则-2022版 ▼] [应用模板]     ││
│  │   ├─ 文书档案著录规则-2022版  ← 默认选择，已实现        ││
│  │   ├─ 文书档案著录规则-1999版  ← 暂不实现                ││
│  │   └─ 文书档案著录规则-1985版  ← 暂不实现                ││
│  │                                                         ││
│  │ 数据列规则列表:                                         ││
│  │ ┌────────┬──────────────────────────────────────────┐  ││
│  │ │ 列名   │ 检查项                                    │  ││
│  │ ├────────┼──────────────────────────────────────────┤  ││
│  │ │ 档号   │ ☑非空 ☑唯一 ☑正则:[^[A-Z]\d{6}$]        │  ││
│  │ │ 题名   │ ☑非空                                    │  ││
│  │ │ 日期   │ ☑非空 ☑日期格式:[yyyy-MM-dd]             │  ││
│  │ │ 责任者 │ ☑非空 ☐值域:[选择...]                    │  ││
│  │ └────────┴──────────────────────────────────────────┘  ││
│  │ [+ 添加列规则]                                          ││
│  │                                                         ││
│  │ 值域管理: [新建值域...] [编辑值域...]                    ││
│  │  - 可自定义值域：名称 + 值列表                          ││
│  └─────────────────────────────────────────────────────────┘│
│                                                             │
└─────────────────────────────────────────────────────────────┘

// 切换到 [文件资源关联检查] Tab 时显示:
┌─────────────────────────────────────────────────────────────┐
│  ┌─ 文件资源关联检查 ──────────────────────────────────────┐│
│  │                                                         ││
│  │ 资源定位方式:                                           ││
│  │   ○ 按档号拼接路径: 基础路径 [____________] + 档号列 [▼]││
│  │   ● 按路径列: [路径 ▼]                                  ││
│  │                                                         ││
│  │ 文件夹级检查:                                           ││
│  │   ☑ 文件夹存在性                                       ││
│  │   ☑ 数据条目存在性（资源有对应数据行）                   ││
│  │   ☑ 文件夹命名格式: [____________]                      ││
│  │   ☑ 文件夹顺序连续                                     ││
│  │                                                         ││
│  │ 文件级检查:                                             ││
│  │   ☑ 文件数量一致: 页数列 [页数 ▼]                       ││
│  │   ☐ 文件名格式: [^\d{4}\.(tif|jpg)$]                   ││
│  │   ☑ 文件序号连续                                       ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘

// 切换到 [内容比对检查] Tab 时显示:
┌─────────────────────────────────────────────────────────────┐
│  ┌─ 内容比对检查 ──────────────────────────────────────────┐│
│  │                                                         ││
│  │ ⚠ 需要 Jinshu-aimp 信息抽取服务                         ││
│  │ 服务状态: [● 已连接] / [○ 未连接 - 点击配置]             ││
│  │                                                         ││
│  │ 规则模板: [文书档案要素比对规则 ▼] [应用模板]        ││
│  │   └─ 文书档案要素比对规则  ← 选择后自动填入4个关键要素   ││
│  │      (后续可扩展其他预置规则模板)                        ││
│  │                                                         ││
│  │ 比对规则列表:                                           ││
│  │ ┌────────────┬────────────┬──────────┬────────┐        ││
│  │ │ 数据列     │ 抽取标签    │ 相似度阈值│ 操作   │        ││
│  │ ├────────────┼────────────┼──────────┼────────┤        ││
│  │ │ 题名(Title)│ 题名        │ 90%      │ [删除] │        ││
│  │ │ 责任者     │ 责任者      │ 90%      │ [删除] │        ││
│  │ │ 文号       │ 文号        │ 95%      │ [删除] │        ││
│  │ │ 成文时间   │ 成文时间    │ 100%     │ [删除] │        ││
│  │ └────────────┴────────────┴──────────┴────────┘        ││
│  │ [+ 添加比对规则]                                        ││
│  │                                                         ││
│  │ 相似度说明:                                             ││
│  │  - 100%: 完全匹配                                       ││
│  │  - ≥阈值: 通过   <阈值且>50%: 警告   <50%: 错误         ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

**预置规则模板说明**：

| 模板名称 | 适用场景 | 自动填充内容 | 状态 |
|----------|----------|--------------|------|
| 文书档案著录规则-2022版 | 数据格式检查 | 档号、题名、日期、责任者等字段规则 | ✅ 已实现 |
| 文书档案著录规则-1999版 | 数据格式检查 | - | 🔜 暂不实现 |
| 文书档案著录规则-1985版 | 数据格式检查 | - | 🔜 暂不实现 |
| 文书档案要素比对规则 | 内容比对检查 | 题名、责任者、文号、成文时间 4个关键要素 | ✅ 已实现 |

**扩展机制**：
- 预置模板以 JSON 配置文件形式存储在扩展目录
- 后续可通过添加新的模板 JSON 文件扩展支持更多规则模板
- 用户选择模板后自动填充，仍可手动调整

### 5.2 检查结果 Tab

**支持分页显示**，避免大数据量时性能问题：

```
┌─────────────────────────────────────────────────────────────┐
│ [数据] [质量规则] [检查结果]                                  │
├─────────────────────────────────────────────────────────────┤
│ 检查统计                                 任务状态: ✓ 已完成  │
│ ┌──────────┬──────────┬──────────┬──────────┐               │
│ │ 总行数    │ 错误     │ 警告      │ 通过     │               │
│ │ 10000    │ 500(5%)  │ 200(2%)  │ 9300(93%)│               │
│ └──────────┴──────────┴──────────┴──────────┘               │
│                                                             │
│ [饼图：通过/错误/警告比例]                                    │
│                                                             │
│ [导出Excel(带标注)] [导出统计报表] [导出PDF报告]               │
├─────────────────────────────────────────────────────────────┤
│ 问题列表    筛选: [全部▼] 级别:[全部▼] 类型:[全部▼] 列:[全部▼]│
│ ┌────┬────────┬────────┬────────┬─────────────────────────┐│
│ │ 行 │ 列     │ 级别   │ 类型   │ 问题描述                 ││
│ ├────┼────────┼────────┼────────┼─────────────────────────┤│
│ │ 3  │ 档号   │ 错误   │ 唯一性 │ 值重复，与第5行相同      ││
│ │ 5  │ 档号   │ 错误   │ 唯一性 │ 值重复，与第3行相同      ││
│ │ 12 │ 日期   │ 错误   │ 格式   │ 不符合日期格式yyyy-MM-dd ││
│ │ 25 │ 路径   │ 警告   │ 资源   │ 文件数量差异(期望:10,实际:9)│
│ │ 30 │ 题名   │ 警告   │ 比对   │ 相似度78%，低于阈值90%   ││
│ └────┴────────┴────────┴────────┴─────────────────────────┘│
│                                                             │
│ 显示 [50▼] 条/页   共 700 条   [<] 1 2 3 ... 14 [>]         │
│                                                             │
│ 💡 点击行号跳转到数据视图                                     │
└─────────────────────────────────────────────────────────────┘
```

**分页实现要点**：
- 默认每页显示 50 条，可选 20/50/100/200
- 支持按级别（错误/警告）、类型、列名筛选
- 筛选后重新计算分页
- 后端 API 支持 `offset` 和 `limit` 参数

### 5.3 数据质量一级导航Tab

在首页一级导航栏（`Refine.actionAreas`）中添加独立的"数据质量"Tab，提供集中的质量任务和规则管理界面。

#### 5.3.1 整体布局

```
┌─────────────────────────────────────────────────────────────────────┐
│ [JinRefine Logo]                                                    │
│                                                                     │
├── 左侧导航 Tab ────────────────────────────────────────────────────┤
│ [创建项目]      ← 原有                                              │
│ [打开项目]      ← 原有                                              │
│ [导入项目]      ← 原有                                              │
│ [数据质量]      ← 新增                                              │
│ [语言设置]      ← 原有                                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ ┌─────────────────── 数据质量管理页面 ─────────────────────────────┐│
│ │                                                                   ││
│ │ ┌─ 二级Tab ─────────────────────────────────────────────────────┐││
│ │ │ [项目质量任务] [全局规则管理] [配置设置]                       │││
│ │ └────────────────────────────────────────────────────────────────┘││
│ │                                                                   ││
│ │ (二级Tab内容区)                                                   ││
│ │                                                                   ││
│ └───────────────────────────────────────────────────────────────────┘│
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

#### 5.3.2 Tab 1: 项目质量任务

显示所有项目的质量任务状态，支持任务控制和快捷操作。

```
┌─────────────────────────────────────────────────────────────────────┐
│ [项目质量任务] [全局规则管理] [配置设置]                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ ┌─ 项目质量任务 ───────────────────────────────────────────────────┐│
│ │                                                                   ││
│ │ 项目搜索: [________________] [🔍]                                 ││
│ │                                                                   ││
│ │ ┌─────────┬─────────────┬───────────────┬───────────────────┐    ││
│ │ │ 项目名称 │ 最后检查时间 │ 任务状态       │ 操作             │    ││
│ │ ├─────────┼─────────────┼───────────────┼───────────────────┤    ││
│ │ │ 土地确权 │ 2025-12-05  │ ████░░ 75%    │ [⏸] [⏹] [📋]    │    ││
│ │ │ 人事档案 │ 2025-12-04  │ ✓ 完成(5错误) │ [▶] [📋] [📊]   │    ││
│ │ │ 财务报表 │ -           │ 无任务        │ [▶] [📋]         │    ││
│ │ └─────────┴─────────────┴───────────────┴───────────────────┘    ││
│ │                                                                   ││
│ │ 图例: [▶]启动检查 [⏸]暂停 [⏹]停止 [📋]编辑规则 [📊]查看报告     ││
│ │                                                                   ││
│ │ 💡 点击项目名称进入项目详情，编辑规则或查看检查结果                ││
│ └───────────────────────────────────────────────────────────────────┘│
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**功能说明**：
| 功能 | 说明 |
|------|------|
| 项目搜索 | 按项目名称过滤 |
| 任务状态 | 显示进度条(运行中)、完成状态(含错误数)、无任务 |
| 启动检查 | 使用项目已配置的规则启动检查 |
| 暂停/恢复 | 暂停或恢复运行中的任务 |
| 停止 | 取消任务执行 |
| 编辑规则 | 跳转到项目的规则编辑界面 |
| 查看报告 | 查看最近一次检查结果 |

#### 5.3.3 Tab 2: 全局规则管理

管理共享规则库，支持规则的创建、编辑、复制和导入导出。

```
┌─────────────────────────────────────────────────────────────────────┐
│ [项目质量任务] [全局规则管理] [配置设置]                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ ┌─ 全局规则管理 ───────────────────────────────────────────────────┐│
│ │                                                                   ││
│ │ 工具栏: [+ 新建规则] [📥 导入规则] [📤 导出选中]                  ││
│ │                                                                   ││
│ │ 搜索: [________________] 类型筛选: [全部 ▼]                       ││
│ │                                                                   ││
│ │ ┌────┬────────────────────┬──────────┬────────────┬─────────────┐││
│ │ │ ☐  │ 规则名称            │ 类型     │ 创建时间    │ 操作        │││
│ │ ├────┼────────────────────┼──────────┼────────────┼─────────────┤││
│ │ │ ☐  │ 文书档案著录规则2022 │ 综合     │ 2025-12-01 │ [✏️][📋][🗑]│││
│ │ │ ☐  │ 档号格式规则        │ 格式检查  │ 2025-11-20 │ [✏️][📋][🗑]│││
│ │ │ ☐  │ 四要素比对规则      │ 内容比对  │ 2025-11-15 │ [✏️][📋][🗑]│││
│ │ │ ☐  │ 文件完整性规则      │ 资源检查  │ 2025-11-10 │ [✏️][📋][🗑]│││
│ │ └────┴────────────────────┴──────────┴────────────┴─────────────┘││
│ │                                                                   ││
│ │ [✏️]编辑 [📋]复制 [🗑]删除                                        ││
│ │                                                                   ││
│ │ 已选中 0 项  共 4 条规则                                          ││
│ │                                                                   ││
│ ├───────────────────────────────────────────────────────────────────┤│
│ │ ┌─ 规则详情/编辑区 ──────────────────────────────────────────────┐││
│ │ │                                                                 │││
│ │ │ 规则名称: [文书档案著录规则2022________________]                 │││
│ │ │ 规则描述: [适用于2022年档案著录规范____________]                 │││
│ │ │                                                                 │││
│ │ │ ┌─ 规则内容Tab ───────────────────────────────────────────────┐│││
│ │ │ │ [格式检查] [资源检查] [内容比对]                              ││││
│ │ │ ├───────────────────────────────────────────────────────────────┤│││
│ │ │ │ (与项目内规则编辑Tab相同的UI，复用组件)                        ││││
│ │ │ │                                                               ││││
│ │ │ │ 数据列规则列表:                                               ││││
│ │ │ │ ┌────────┬──────────────────────────────────────────┐        ││││
│ │ │ │ │ 列名   │ 检查项                                    │        ││││
│ │ │ │ ├────────┼──────────────────────────────────────────┤        ││││
│ │ │ │ │ 档号   │ ☑非空 ☑唯一 ☑正则:[^[A-Z]\d{6}$]        │        ││││
│ │ │ │ │ 题名   │ ☑非空                                    │        ││││
│ │ │ │ └────────┴──────────────────────────────────────────┘        ││││
│ │ │ │ [+ 添加列规则]                                                ││││
│ │ │ └───────────────────────────────────────────────────────────────┘│││
│ │ │                                                                 │││
│ │ │                              [取消] [保存规则]                   │││
│ │ └─────────────────────────────────────────────────────────────────┘││
│ └───────────────────────────────────────────────────────────────────┘│
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**功能说明**：
| 功能 | 说明 |
|------|------|
| 新建规则 | 创建新的全局共享规则 |
| 导入规则 | 从JSON文件导入规则配置 |
| 导出选中 | 将选中规则导出为JSON文件 |
| 编辑 | 在下方详情区编辑规则内容 |
| 复制 | 基于现有规则创建副本 |
| 删除 | 删除规则（需确认） |
| 类型筛选 | 按规则类型过滤：综合/格式检查/资源检查/内容比对 |

#### 5.3.4 Tab 3: 配置设置

管理数据质量检查相关的全局配置。

```
┌─────────────────────────────────────────────────────────────────────┐
│ [项目质量任务] [全局规则管理] [配置设置]                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ ┌─ 配置设置 ───────────────────────────────────────────────────────┐│
│ │                                                                   ││
│ │ ┌─ AIMP服务配置 ─────────────────────────────────────────────────┐││
│ │ │                                                                 │││
│ │ │ 服务地址:  [http://127.0.0.1:7998___________] [🔗 测试连接]     │││
│ │ │                                                                 │││
│ │ │ 连接状态:  ● 已连接 (响应时间: 45ms)                            │││
│ │ │           ○ 未连接 - 请检查服务是否启动                         │││
│ │ │                                                                 │││
│ │ │ 请求超时:  [30____] 秒                                          │││
│ │ │                                                                 │││
│ │ └─────────────────────────────────────────────────────────────────┘││
│ │                                                                   ││
│ │ ┌─ 任务执行配置 ─────────────────────────────────────────────────┐││
│ │ │                                                                 │││
│ │ │ 线程池大小:      [4______] 个                                   │││
│ │ │                  (并发执行的最大任务数，建议: CPU核心数)          │││
│ │ │                                                                 │││
│ │ │ 批处理大小:      [100____] 行                                   │││
│ │ │                  (每批处理的行数，影响断点保存频率)               │││
│ │ │                                                                 │││
│ │ │ 自动保存间隔:    [60_____] 秒                                   │││
│ │ │                  (任务运行时自动保存checkpoint的间隔)            │││
│ │ │                                                                 │││
│ │ └─────────────────────────────────────────────────────────────────┘││
│ │                                                                   ││
│ │ ┌─ 内容比对配置 ─────────────────────────────────────────────────┐││
│ │ │                                                                 │││
│ │ │ 默认相似度阈值:                                                 │││
│ │ │                                                                 │││
│ │ │   通过阈值:   [100____] %   (≥此值判定为通过)                   │││
│ │ │   警告阈值:   [100____] %   (≥此值且<通过阈值判定为警告)        │││
│ │ │   错误判定:   < 警告阈值自动判定为错误                          │││
│ │ │                                                                 │││
│ │ │ 批量比对大小:  [5______] 条                                     │││
│ │ │                (每次调用AIMP比对的记录数)                        │││
│ │ │                                                                 │││
│ │ │ OCR置信度阈值: [0.8____]                                        │││
│ │ │                (低于此置信度的抽取结果标记为不可信)               │││
│ │ │                                                                 │││
│ │ └─────────────────────────────────────────────────────────────────┘││
│ │                                                                   ││
│ │ ┌─ 界面配置 ─────────────────────────────────────────────────────┐││
│ │ │                                                                 │││
│ │ │ 任务列表刷新间隔:  [5______] 秒                                 │││
│ │ │                    (运行中任务状态的轮询频率)                    │││
│ │ │                                                                 │││
│ │ │ 每页显示条数:      [50_____] 条                                 │││
│ │ │                    (检查结果列表每页显示数量)                    │││
│ │ │                                                                 │││
│ │ └─────────────────────────────────────────────────────────────────┘││
│ │                                                                   ││
│ │                     [恢复默认值]              [保存配置]           ││
│ │                                                                   ││
│ │ 💡 配置修改后对新启动的任务生效，运行中的任务不受影响              ││
│ │                                                                   ││
│ └───────────────────────────────────────────────────────────────────┘│
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**配置项说明**：
| 分组 | 配置项 | 默认值 | 说明 |
|------|--------|--------|------|
| **AIMP服务** | 服务地址 | `http://127.0.0.1:7998` | OCR服务API地址 |
| | 请求超时 | 30秒 | 单次请求超时时间 |
| **任务执行** | 线程池大小 | 4 | 并发任务数 |
| | 批处理大小 | 100行 | 断点保存粒度 |
| | 自动保存间隔 | 60秒 | checkpoint保存频率 |
| **内容比对** | 通过阈值 | 100% | 相似度≥此值为通过 |
| | 警告阈值 | 100% | 相似度≥此值为警告 |
| | 批量比对大小 | 5条 | AIMP批量请求大小 |
| | OCR置信度阈值 | 0.8 | 低于此值结果不可信 |
| **界面** | 刷新间隔 | 5秒 | 任务状态轮询频率 |
| | 每页显示 | 50条 | 结果分页大小 |

## 6. PDF 报告设计

推荐使用**饼图**作为主要图表类型，原因：
1. 直观展示通过/错误/警告的比例关系
2. 简洁易读，适合汇总报告
3. 易于实现，主流 PDF 库都支持

PDF 报告结构：
```
┌─────────────────────────────────────────────────────────────┐
│                    数据质量检查报告                          │
│─────────────────────────────────────────────────────────────│
│ 项目名称: XXX                检查时间: 2025-12-05 10:00     │
│ 规则名称: 档案数据质量规则                                   │
│─────────────────────────────────────────────────────────────│
│                        检查概览                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                                                       │    │
│  │            [饼图：通过93% 错误5% 警告2%]             │    │
│  │                                                       │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│ 总行数: 1000  |  通过: 930  |  错误: 50  |  警告: 20        │
│─────────────────────────────────────────────────────────────│
│                      错误分类统计                            │
│  唯一性检查:  15 个错误                                      │
│  非空检查:    10 个错误                                      │
│  格式检查:    12 个错误                                      │
│  资源检查:    13 个错误                                      │
│─────────────────────────────────────────────────────────────│
│                      详细错误列表                            │
│  行3  | 档号   | 唯一性 | 值重复，与第5行相同               │
│  行5  | 档号   | 唯一性 | 值重复，与第3行相同               │
│  ...                                                         │
└─────────────────────────────────────────────────────────────┘
```

## 7. 实现计划

### 7.1 第一阶段：基础框架（预计 3 天） ✅ 完成

1. 创建 `data-quality` 扩展目录结构 
2. 实现 ExtensionBar 菜单注册
3. 实现 tool-panel Tab 扩展机制
4. 创建基础 UI 框架

### 7.2 第二阶段：规则编辑（预计 3 天） ✅ 完成

1. 实现质量规则 Tab UI
2. 实现规则保存/加载 API（支持共享）
3. 实现管理规则对话框

### 7.3 第三阶段：格式检查（预计 2 天） ✅ 完成

1. 实现唯一性检查
2. 实现非空检查
3. 实现值域检查（值列表、日期格式、正则）

### 7.4 第四阶段：资源检查（预计 2 天） ✅ 完成

1. 实现文件夹存在性检查
2. 实现文件数量一致性检查
3. 实现文件名格式检查
4. 实现文件序号连续性检查

### 7.5 第五阶段：内容比对（预计 3 天） ✅ 完成

1. 实现 Jinshu-aimp 客户端集成
2. 实现 OCR 内容抽取
3. 实现内容比对逻辑

### 7.6 第六阶段：结果展示与导出（预计 3 天） ✅ 完成

1. 实现检查结果 Tab（含饼图）
2. 实现单元格错误标注
3. 实现 Excel 导出（带标注）
4. 实现 PDF 报告导出（含饼图）

### 7.7 第七阶段：任务管理与数据质量导航Tab（预计 4 天）✅ 已完成

1. ✅ 实现异步任务执行框架
   - 后台线程池管理（CachedThreadPool）
   - 任务状态机（PENDING → RUNNING → PAUSED/COMPLETED/FAILED/CANCELLED）
2. ✅ 实现断点续跑机制
   - 批次处理（默认 100 行/批）
   - checkpoint 自动保存（每批次完成后）
   - 暂停时立即保存当前状态
   - 恢复时从 checkpoint 继续
   - FormatChecker/ResourceChecker/ContentChecker 均支持中断和断点续跑
3. ✅ **实现数据质量一级导航Tab**（需求变更：原为项目列表任务列）
   - Tab 1: 项目质量任务 - 显示所有项目任务状态，支持启动/暂停/停止控制
   - Tab 2: 全局规则管理 - 管理共享规则库，支持新建/编辑/复制/导入导出
   - Tab 3: 配置设置 - AIMP服务/任务执行/内容比对/界面配置（相似度阈值默认100%）
4. ✅ 任务控制API
   - TaskControlCommand: pause/resume/cancel 操作
   - ListTasksCommand: 列出所有项目任务
   - GetConfigCommand/SaveConfigCommand: 配置管理
   - TestAimpConnectionCommand: AIMP连接测试
5. ✅ 前端任务控制
   - run-check-dialog 添加暂停/恢复/取消按钮
   - 实时进度轮询和状态更新

## 8. 依赖项

- OpenRefine 核心框架
- wikibase 扩展（参考实现）
- records-assets 扩展（文件路径处理）
- Jinshu-aimp 服务（OCR 和内容抽取）
- Apache POI（Excel 导出）
- iText 或 Flying Saucer（PDF 生成）
- JFreeChart 或类似库（饼图生成）

## 9. 配置项 ❌ 未完成

在项目根目录 `refine.ini` 中添加默认配置，通过 `JAVA_OPTIONS` 传递给 JVM：

```ini
# Jinshu-aimp 服务地址
JAVA_OPTIONS=-Djinshu.aimp.server=http://127.0.0.1:8080 -Dquality.check.thread.pool.size=4 -Dquality.check.batch.size=100
```

代码中通过 `Configurations.get()` 读取：

```java
// 读取配置，支持系统属性和环境变量
String aimpServer = Configurations.get("jinshu.aimp.server",
    System.getenv().getOrDefault("JINSHU_AIMP_SERVER", "http://127.0.0.1:8080"));
```

如无法连接时仍保留自定义输入服务地址的功能

### 配置项说明

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `jinshu.aimp.server` | `http://127.0.0.1:7998` | Jinshu-aimp OCR 服务地址 |
| `jinshu.aimp.timeout` | `30` | AIMP请求超时时间（秒） |
| `quality.check.thread.pool.size` | `4` | 质量检查并发线程数 |
| `quality.check.batch.size` | `100` | 批量检查每批处理行数（断点保存粒度） |
| `quality.check.autosave.interval` | `60` | 任务checkpoint自动保存间隔（秒） |
| `quality.content.similarity.pass` | `1.0` | 内容比对通过阈值（100%，可在规则中覆盖） |
| `quality.content.similarity.warning` | `1.0` | 内容比对警告阈值（100%，低于此值为错误） |
| `quality.content.batch.size` | `5` | AIMP批量比对每批记录数 |
| `quality.content.ocr.confidence` | `0.8` | OCR置信度阈值（低于此值结果不可信） |
| `quality.ui.refresh.interval` | `5` | 任务列表刷新间隔（秒） |
| `quality.ui.page.size` | `50` | 检查结果列表每页显示数量 |

## 10. 实现总结

### 10.1 关键设计决策

| 决策项 | 决定 | 原因 |
|--------|------|------|
| 独立扩展 | 作为 `data-quality` 独立扩展 | 解耦，可单独部署和维护 |
| 规则共享 | 支持项目间共享规则 | 存储在 `workspace/record-db/quality-rules/` |
| 任务持久化 | 持久化到项目目录 | 项目数据自包含，便于备份迁移 |
| 断点续跑 | 批次处理 + checkpoint | 支持大数据量，可中断恢复 |
| 警告级别 | 相似度阈值判定 | 区分"明确错误"和"需复核"场景 |
| PDF 图表 | 饼图 | 直观展示通过/错误/警告比例 |
| 分页查询 | 默认 50 条/页 | 避免大数据量性能问题 |

### 10.2 预计工期

| 阶段 | 工作内容 | 预计天数 |
|------|----------|----------|
| 第一阶段 | 基础框架 | 3 天 |
| 第二阶段 | 规则编辑（含二级 Tab） | 3 天 |
| 第三阶段 | 格式检查 | 2 天 |
| 第四阶段 | 资源检查 | 2 天 |
| 第五阶段 | 内容比对 | 3 天 |
| 第六阶段 | 结果展示与导出（含分页） | 3 天 |
| 第七阶段 | 任务管理与断点续跑 | 3 天 |
| **合计** | | **19 天** |
